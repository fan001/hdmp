package com.hd.hdmp.repository.task;

import com.hd.hdmp.entity.task.WeworkApproveModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WeworkApproveRepository extends JpaRepository<WeworkApproveModel,Long> {
}